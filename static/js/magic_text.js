const typed = new Typed('.multiple-text', {
  strings: ['Estudante da área de TI', 'Desenvolvedor Backend.'],
  typeSpeed: 70,
  backSpeed: 70,
  backDelay: 1000,
  loop: true,
});

const vai = new Typed('.passar', {
  strings: ['Passe para o lado ->'],
  typeSpeed: 70,
  backSpeed: 70,
  backDelay: 3000,
  loop: true,
});